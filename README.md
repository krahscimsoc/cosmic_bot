A pretty simple discord bot, it has only a few functions. 
In order to use the bot, you need to download discord.py(it's an API, which the bot uses).

First things first, in order to connect the code to the bot itself. You do that by swapping the "Your bot token" text in line 93 for your actual token. Next, you need to swap the "Your guild ID" text for the ID of the guild you want to use the bot in (I haven't tested it in multiple servers yet).

The current functions (and commands which you can use) of the bot are:

1. It shows every message sent in the guild(along with who wrote them) in the terminal.
2. If someone reacts to a message with an emote, the bot will print out a text saying 
